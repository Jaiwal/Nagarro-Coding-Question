#include<bits/stdc++.h>
using namespace std;
int count(int n)
{
	double tsq=0;
	int unti=0;
	 
	 while(n>tsq)
	 {
	 	unti++;
	 	tsq+=12*pow(unti,2);
	 }
	 return unti*8;
}
int main()
{
	int n;
	cin>>n;
	cout<<count(n)<<endl;
}
