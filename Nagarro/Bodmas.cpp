#include<iostream>
#include<cstring>
#include<stack>
using namespace std;
int bodmas(string input)
{
    stack<char> stk;
    stk.push(input[0]);
    while(!stk.empty())
    {
        if(input[i])
    }
}
int main()
{
    string input;
    cin>>input;
    cout<<bodmas(input);
    return 0;
}
